{
  "name": "domain-os",
  "version": "0.0.0",
  "private": true,
  "license": "AGPL-3.0-only",
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "postinstall": "node scripts/ensure-native.mjs node",
    "pretest": "node scripts/ensure-native.mjs node",
    "predev": "node scripts/ensure-native.mjs electron && npm run build --workspace=@domain-os/core && npm run build --workspace=@domain-os/integrations",
    "dev": "npm run dev --workspace=@domain-os/desktop",
    "build": "npm run build --workspace=@domain-os/core && npm run build --workspace=@domain-os/integrations && npm run build --workspace=@domain-os/desktop",
    "test": "npm test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "clean": "npm run clean --workspaces --if-present && rm -rf node_modules"
  }
}
